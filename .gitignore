# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# SECURITY-FIX (P-M3): never commit environment files (may hold VITE_* secrets)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# vite-plugin-pwa dev-mode service worker output — regenerated on every `npm run dev`,
# so it produced a spurious diff on every branch. Build output does not belong in git.
dev-dist/
